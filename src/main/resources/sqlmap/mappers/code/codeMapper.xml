<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bx.bxbms.code.dao.CodeMapper">
	
		<!-- 공통코드그룹 등록 -->
		<insert id="insertCodeGrp" parameterType="codeVO">
			INSERT INTO BX_CODE_GRP_M(
			  CODE_GRP_ID
			, CODE_GRP_NM
			, CODE_GRP_DESC
			, REG_ID
			, REG_DT
			, DEL_YN
			)VALUES(
			  #{codeGrpId}
			, #{codeGrpNm}
			, #{codeGrpDesc}
			, #{regId}
			, #{regDt}
			, 'N'
			)
		</insert>
		
		<!-- 공통코드 그룹 목록 -->
		<select id="codeGrpList" parameterType="codeVO" resultType="codeVO" >
			SELECT 
				CODE_GRP_ID
				, CODE_GRP_NM 
				, CODE_GRP_DESC
			FROM
			 BX_CODE_GRP_M
		</select>
		
		<select id="codeGrpDetail" parameterType="codeVO" resultType="codeVO" >
			SELECT
				CODE_GRP_ID ,
				CODE_GRP_NM ,
				CODE_GRP_DESC,
				REG_ID,
				REG_DT,
				MDFCN_DT,
				MDFCN_ID,
				MDFCN_DT,
				DEL_YN
			FROM
				BX_CODE_GRP_M
			WHERE
				CODE_GRP_ID = #{codeGrpId}
		</select>
	
</mapper>